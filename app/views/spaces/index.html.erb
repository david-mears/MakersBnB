<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Look at all the places!</h1>

    <% @spaces.each do |space| %>
    <%= "#{space.name}" %><br />
    <%= "#{space.description}" %><br />
    <%= "£#{space.price} per night" %><br />
    <%= "#{@users.find(space.user_id).forename}" %><br />
    <p> Availability </p><br />
    <%= "#{@availabilities.find(space.id).date}" %>
    <br />
    <br />

    <% end %>
  </body>
</html>
