<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Spaces</title>
    <link rel="stylesheet" type="text/css" href="showlisting.css">
    <link rel="stylesheet" href="../../assets/stylesheets/spaces.css">
  </head>
  <body>
    <%= render 'shared/menu' %>
    <div class="container">
      <div class="top1"></div>
      <div class="top2"><h1 class="heading">MakersBnB</h1> </div>
      <div class="top3"></div>

    <div class="cardbg">
      <div class="imagecontainer"><img src="https://source.unsplash.com/collection/416011/300x300" ></div>
        <div class="placename">
          <p class="textname"><%= "#{@space[:name]}" %></p>
        </div>
        <div class="price">
          <p class="textprice"><%= "£#{@space[:price]} per night" %></p>
        </div>
        <div class="placedescription">
          <p class="textdescription"><%= "#{@space[:description]}" %></p>
          <p class="textdescription">Dates available:</p>
              <%= form_with model: @availability, url: {action: "create"}, local: true do |f| %>
              <% @dates.each do |date| %>
                <p class="textdescription"><%= f.radio_button :availability, date %>
                  <% unless @date.nil? %>
                  <%= date.strftime("%A, %e %B %Y") %></p>
                  <% end %>
              <% end %>
              <p class="buttondescription"><%= f.submit "Book", class: "btn btn-primary" %></p>
            <% end %>
          </div>
        </div>
</body>
</html>
